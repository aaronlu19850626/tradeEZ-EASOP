# TradeEZ-SOP 第 2 项：独立数据同步 EA 设计

> 状态：设计已确认，进入开发  
> 日期：2026-09-18（北京时间）  
> 本阶段先确认架构，不修改交易代码。

## 1. 目标

将全部网络请求从 `tradeEZ.mq5` 移至独立的 `tradeEZSync.mq5`，使网络连接、接口超时、服务器错误和数据重传均不能阻塞交易 EA 的 Tick、Timer、图表操作和持仓管理。

完成后必须满足：

- 主交易 EA 中不存在 `WebRequest()` 调用；
- 同步 EA 永远不下单、改单、减仓或平仓；
- 同步 EA 被移除、卡住、断网或配置错误时，交易 EA 继续正常管理持仓；
- 主交易 EA 不再提供手动同步、同步状态或任何网络配置；
- 手动同步按钮、确认提示和执行状态全部由同步 EA 自己提供；
- 同步服务恢复后可以安全补传，重复数据由服务器幂等处理。

## 2. 部署模型

### 2.1 推荐模型

一个 MT5 交易账户只运行一个同步服务 EA：

```text
任意普通图表
└─ tradeEZSync.mq5（账户级唯一）

交易图表 A
└─ tradeEZ.mq5（实例 A）

交易图表 B
└─ tradeEZ.mq5（实例 B）
```

同步 EA 负责整个账户的成交、账户快照和心跳，同时读取各交易 EA 实例发布的品种与参数清单。

### 2.2 单实例约束

- 同步 EA 启动时获取账户级租约；同账户第二个同步 EA 只能显示“已有同步服务运行”，不得重复上传。
- 租约包含账号、服务器标识和最后心跳时间。
- 原同步 EA 超过规定时间没有更新租约，新实例才能接管。
- 租约只用于选举同步服务，不参与任何交易决策。

## 3. 职责边界

| 能力 | `tradeEZ.mq5` | `tradeEZSync.mq5` |
|---|---:|---:|
| 市价/限价下单 | 是 | 禁止 |
| 修改 SL/TP | 是 | 禁止 |
| 平仓/撤单 | 是 | 禁止 |
| 动态追踪与风控 | 是 | 禁止 |
| 发布实例参数摘要 | 是 | 读取 |
| 发起手动同步请求 | 不提供 | 在自身面板确认并执行 |
| 读取账户成交历史 | 交易统计需要时读取 | 同步上传时读取 |
| WebRequest | 禁止 | 是 |
| API 密钥 | 不保存 | 仅同步 EA 输入参数保存 |
| 显示同步状态 | 不显示 | 在自身面板显示 |

## 4. 本地通信协议

通信分为快速状态和完整配置两类。

### 4.1 终端全局变量：快速状态

终端全局变量只保存数值，不保存密钥或完整业务数据。键名逻辑如下：

```text
TEZ.SYNC.<account>.<server_hash>.lease
TEZ.SYNC.<account>.<server_hash>.heartbeat_utc
TEZ.SYNC.<account>.<server_hash>.status
TEZ.SYNC.<account>.<server_hash>.last_success_utc
TEZ.SYNC.<account>.<server_hash>.last_error
```

状态值：

| 值 | 含义 |
|---:|---|
| 0 | 服务未连接或未知 |
| 1 | 空闲 |
| 2 | 正在同步 |
| 3 | 最近一次同步失败 |
| 4 | 配置错误 |
| 5 | 已停用 |

这些变量仅用于同步 EA 的账户级单例租约、运行诊断和故障接管，主交易 EA 不读取它们，也不据此改变交易行为。

### 4.2 本地文件：实例清单与配置

每个交易 EA 实例发布一份版本化清单：

```text
MQL5/Files/TradeEZ/instances/<account>/<instance_id>.json
```

建议字段：

```json
{
  "schema_version": 1,
  "instance_id": "gold-main",
  "account_login": 12345678,
  "server": "Broker-Server",
  "symbol": "GOLD#",
  "ea_version": "1.03",
  "updated_utc": 1789740000,
  "settings": {}
}
```

写入规则：先写临时文件并刷新、关闭，再替换正式文件，避免同步 EA 读取到半份 JSON。清单超时后标记为离线，但不立即删除。

### 4.3 同步 EA 的手动同步按钮

主 EA 完全移除手动同步入口。同步 EA 自己提供非模态两步确认：

```text
点击“同步数据”
→ 面板显示风险和数据范围说明
→ 点击“确认同步”或“取消”
→ 确认后由同步 EA 排队执行
```

同步按钮只存在于同步 EA 面板。同步配置关闭或密钥为空时按钮禁用，并给出明确配置提示。即使同步 EA 的确认或网络请求发生阻塞，也不会占用主交易 EA 的事件线程。

## 5. 同步任务状态机

```text
DISABLED
   ↓
IDLE
   ↓  定时、每日或手动请求
QUEUED
   ↓
SYNCING_SYMBOLS
   ↓
SYNCING_DEALS
   ↓
SYNCING_SNAPSHOT
   ↓
SYNCING_SETTINGS
   ↓
SYNCING_HEARTBEAT
   ↓
SUCCESS → IDLE

任一步失败 → RETRY_WAIT → 从安全检查点重试
```

规则：

- 同一时刻只运行一个同步批次；
- 手动请求可以提升已有待处理任务优先级，但不并发执行第二批；
- 每个网络请求都有独立超时；
- 连续失败采用退避重试，例如 5 秒、15 秒、60 秒、5 分钟；
- 成交上传失败不得推进游标；
- 部分批次成功后可以安全重传，依赖成交 ticket 幂等去重；
- UI 空闲不等于上次同步成功，必须单独显示最近结果和时间。

## 6. 调度规则

拆分后网络不会阻塞交易 EA，因此建议：

| 任务 | 推荐周期 | 有持仓时 |
|---|---:|---:|
| 成交增量 | 1–5 分钟 | 允许 |
| 账户快照 | 30 秒 | 允许 |
| 心跳 | 5 分钟 | 允许 |
| 参数清单 | 启动、配置变化及每小时校验 | 允许 |
| 品种规格 | 实例出现、规格变化及每日校验 | 允许 |
| 每日完整同步 | 北京时间统计日切换后一次 | 允许 |
| 手动同步 | 用户确认后 | 允许 |

原来的“账户有持仓不自动同步”是为了避免主 EA 被阻塞。拆分后建议取消该限制，否则持仓持续存在时复盘数据会长期延迟。

## 7. 增量成交协议修正

### 7.1 当前矛盾

现有接口文档将 `last_sync_time` 定义为最大开仓时间，当前客户端却按平仓时间筛选已关闭 position，并用平仓时间推进同一个游标。两种语义不能混用。

使用开仓时间作为“新平仓数据”游标存在漏单风险：一笔很早开仓、很晚平仓的持仓，可能在游标已经越过其开仓时间后才结束。

### 7.2 推荐新规则

同步对象改为全部新增成交，而不是等待整笔 position 完全关闭：

- 游标使用 `deal_time_msc`，并保留 `deal_ticket` 作为同毫秒边界；
- 查询采用包含式边界，允许重复上传边界成交；
- 服务端唯一键至少为账户身份加成交 ticket；
- 开仓、加仓、减仓、平仓和费用成交均按实际出现顺序上传；
- 服务端使用 `position_id` 配对持仓生命周期；
- 客户端不再依赖“回看30天寻找开仓成交”的临时办法。

推荐游标：

```json
{
  "deal_time_msc": 1789740000123,
  "deal_ticket": 560708325
}
```

这需要后端接口同步升级。若后端暂时不能升级，第 2 项只能先做线程隔离兼容版，并将旧游标问题标记为未完成风险，不能宣称增量同步已经完全可靠。

## 8. API 与数据身份

账户身份不能只依赖登录账号，建议服务端至少联合：

```text
租户/用户 + broker_server + account_login
```

设置快照增加：

- `instance_id`；
- `symbol`；
- `ea_version`；
- `schema_version`；
- `settings_hash`。

这样同一账户多个 TradeEZ 图表的参数不会互相覆盖或无法区分。

## 9. 安全规则

- API 密钥只存在同步 EA 参数中，不进入交易 EA、实例清单、普通日志和截图。
- 日志最多显示密钥末四位或密钥指纹，默认完全不显示。
- HMAC 时间戳使用 UTC；服务器允许有限时钟偏差并拒绝过期请求。
- 服务器返回内容限制长度，日志不得无边界打印完整响应。
- 配置错误或鉴权失败不能高频重试。
- 同步 EA 无任何交易函数调用；代码审查和自动搜索应验证这一点。

## 10. 同步 EA 面板状态显示

建议状态文本：

| 状态 | 显示 |
|---|---|
| 服务未启用 | 同步已停用 |
| 空闲且上次成功 | 同步空闲 |
| 正在执行 | 正在同步 |
| 最近失败 | 同步失败 |
| 密钥/白名单错误 | 同步配置错误 |
| 请求已排队 | 等待同步 |

这些状态只显示在同步 EA 自己的图表面板。主交易 EA 不显示同步按钮、同步状态或同步提示。

## 11. 实施顺序

1. 固定本地键名、实例清单 schema 和同步状态机。
2. 新建 `tradeEZSync.mq5`，先迁移 HMAC、HTTP 和账户级上传逻辑。
3. 在主 EA 中仅加入不含密钥的实例参数清单发布。
4. 在同步 EA 自身面板实现手动同步与非模态确认。
5. 从主 EA 删除同步输入密钥、WebRequest函数和网络调度。
6. 编译两个 EA，静态确认主 EA 不含 `WebRequest()`。
7. 在模拟账户测试正常接口、超时、断网、错误密钥和服务卸载。
8. 后端升级游标后验证长持仓、同毫秒成交、重复上传和断点恢复。

## 12. 验收矩阵

| ID | 场景 | 通过条件 |
|---|---|---|
| S-01 | 同步接口正常 | 成交、快照、品种、设置、心跳均成功 |
| S-02 | 接口超时30秒 | 只有同步 EA 等待；主 EA 报价、Tick 管理和按钮正常 |
| S-03 | 同步 EA 被移除 | 主 EA 继续交易管理，主面板无任何同步控件或状态依赖 |
| S-04 | 主 EA 被重载 | 同步 EA 继续运行；实例清单恢复后重新识别 |
| S-05 | 两个同步 EA | 只有一个获得租约并上传 |
| S-06 | 两个交易实例 | 参数和品种按 instance_id 区分，不互相覆盖 |
| S-07 | 手动同步连续点击 | 请求合并或排队，不并发、不阻塞交易 EA |
| S-08 | 断网后恢复 | 按退避重试，恢复后补传且无重复业务记录 |
| S-09 | 错误密钥 | 状态显示配置错误，不泄露密钥，不高频重试 |
| S-10 | 长时间持仓后平仓 | 开平仓成交均不遗漏 |
| S-11 | 同毫秒多个成交 | 所有不同 ticket 均上传，重复 ticket 被幂等处理 |
| S-12 | 同步期间触发追踪 | 主 EA 持仓保护按 Tick 正常执行 |
| S-13 | 主 EA 静态检查 | `tradeEZ.mq5` 中不存在 `WebRequest(` |
| S-14 | 同步 EA 静态检查 | 不存在下单、改单、平仓或撤单调用 |
| S-15 | 编译 | 两个 `.mq5` 均为 0 errors、0 warnings |

## 13. 待确认决策

| 编号 | 推荐方案 | 状态 |
|---|---|---|
| S-D01 | 一个账户运行一个同步 EA，由租约避免重复实例 | 已确认 |
| S-D02 | 拆分后自动同步不再要求空仓 | 已确认 |
| S-D03 | 手动同步及确认界面全部放在同步 EA，不再由主 EA 发起 | 已确认（调整后） |
| S-D04 | 主 EA 完全移除同步按钮、状态显示、网络参数和 API 密钥 | 已确认（调整后） |
| S-D05 | 同步参数全部移动到同步 EA；主 EA 仅发布不含密钥的实例参数清单 | 已确认（调整后） |
| S-D06 | 增量协议升级为 `deal_time_msc + deal_ticket` 游标，并同步全部新增成交 | 已确认，依赖后端升级 |
| S-D07 | 多实例设置上传增加 instance_id、symbol 和版本字段 | 已确认，依赖后端兼容 |
| S-D08 | 同步服务源文件暂时只保存在本地交付，不推送当前 GitHub；GitHub 仍只提交 `tradeEZ.mq5` | 已确认 |

项目所有者已确认 S-D01 至 S-D08，并明确主 EA 不保留任何手动同步能力，进入代码开发。
